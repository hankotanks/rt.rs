import("../pkg/index.js").catch(console.error);

/*
import init from "../pkg/index.js";
init().catch((error) => {
    if (!error.message.startsWith("Using exceptions")) {
        throw error;
    }
}).then(() => {
    console.log("Loaded");
}); */